# Core application dependencies
flask>=2.3.0
flask-cors>=4.0.0

# For deployment and production (optional)
gunicorn>=20.1.0
waitress>=2.1.0

# Development dependencies
python-dotenv>=1.0.0  # For environment configuration

# Note: The following are built-in Python modules:
# sqlite3, hashlib, secrets, time, statistics, threading, 
# json, re, dataclasses, contextlib, logging